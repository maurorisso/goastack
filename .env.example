# GoaStack Environment Variables
# ----------------------------------------
# 1. Create a new Supabase project at https://app.supabase.com
# 2. Get your credentials from: Project Settings > API
# 3. Copy this file to .env (which will be gitignored) and update the values
# 4. Never commit your actual credentials to version control

# Supabase Configuration
# ---------------------
# Project URL: Found in Project Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL=https://[YOUR-PROJECT-ID].supabase.co

# Anon/Public Key: Found in Project Settings > API > Project API keys > anon/public
NEXT_PUBLIC_SUPABASE_ANON_KEY=[YOUR-ANON-KEY]

# Database Connection (Only needed if using direct database access)
# -------------------------------------------------------------
# Found in Project Settings > Database > Connection string > URI
DB_CONNECTION_STRING=postgresql://postgres:[YOUR-DB-PASSWORD]@db.[YOUR-PROJECT-ID].supabase.co:5432/postgres
